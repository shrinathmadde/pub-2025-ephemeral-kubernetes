# ==============================================================================
# NODE REGISTRY
# One line per node: HOSTNAME  MAC_ADDRESS  IP_ADDRESS  CLUSTER  ROLE  [DYNAMIC_STATE]
# Edit this file to add, remove, or reassign nodes.
# Global settings (MANAGER_IP, SLURM_VERSION, etc.) live in cluster-settings.conf
# ==============================================================================

# ==============================================================================
# FIXED KUBERNETES NODES (never reassigned)
# ==============================================================================
# HOSTNAME       MAC                 IP          CLUSTER  ROLE
control0         fa:16:3e:9d:d4:f1   10.0.0.2    k8s      k8s-control
control1         fa:16:3e:75:3e:d8   10.0.0.16   k8s      k8s-control

# ==============================================================================
# FIXED SLURM NODES (never reassigned)
# ==============================================================================
# HOSTNAME       MAC                 IP          CLUSTER  ROLE
slurm-ctrl       fa:16:3e:2c:47:17   10.0.0.14   slurm    slurm-controller
slurm-compute01  fa:16:3e:80:7b:76   10.0.0.12   slurm    slurm-compute

# ==============================================================================
# DYNAMIC POOL (managed by the balancer)
# ==============================================================================
# HOSTNAME       MAC                 IP          CLUSTER  ROLE     DYNAMIC_STATE
dynamic-node   fa:16:3e:e1:54:61  10.0.0.25   dynamic  dynamic  current_cluster=none
#
# ^^^ UNCOMMENT AND EDIT the line above with your actual node's MAC and IP.
#     The balancer reads this and flips current_cluster between k8s and slurm.
# ==============================================================================
